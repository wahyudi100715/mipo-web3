import { PopupCommunication } from '@aave-account/web-elements-bindings';
import { ProxyConfig } from './proxy';
export declare enum PopupSize {
    MEDIUM = "MEDIUM",
    INHERITED = "INHERITED"
}
export declare const openPopup: (url: string, name: string, size?: PopupSize) => Window | null;
export declare const proxyPopup: <T>(popup: Window, request: PopupCommunication, config?: ProxyConfig) => Promise<T>;
