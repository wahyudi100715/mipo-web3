import { Communication, Subscription, SubscriptionTypes } from '@aave-account/web-elements-bindings';
export type ProxyConfig = {
    timeout?: number;
};
export declare const proxy: <T>(request: Communication, config?: ProxyConfig) => Promise<T>;
type ExtractSubscription<T extends SubscriptionTypes> = Extract<Subscription, {
    type: T;
}>;
/**
 * Subscribe to a specific event
 * Supports multiple subscriptions to the same event
 * see https://github.com/krakenjs/post-robot/issues/48
 */
export declare const subscribe: <T extends SubscriptionTypes, D = ExtractSubscription<T>["data"]>(type: T, callback: (data: D) => void) => (() => void);
export {};
