export declare class IntegratedClientModalController {
    private readonly unsubscribe;
    private readonly autoHeight;
    constructor(container: HTMLDivElement, iframe: HTMLIFrameElement);
    disconnect(): void;
}
