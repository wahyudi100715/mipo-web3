import { Client, Hex, JsonRpcAccount, PublicActions, PublicRpcSchema, Transport, WalletActions, WalletRpcSchema } from 'viem';
export type AaveAccountCustomActions = {
    getIdToken(): Promise<string | null>;
    switchAccounts(): Promise<Hex[]>;
    disconnect(): Promise<void>;
};
export declare const aaveAccountCustomActions: (client: Client) => AaveAccountCustomActions;
export type AaveAccountClient = Client<Transport, undefined, JsonRpcAccount, WalletRpcSchema & PublicRpcSchema, WalletActions<undefined, JsonRpcAccount> & PublicActions<Transport, undefined> & AaveAccountCustomActions>;
export declare const createAaveAccountClient: () => Promise<AaveAccountClient>;
