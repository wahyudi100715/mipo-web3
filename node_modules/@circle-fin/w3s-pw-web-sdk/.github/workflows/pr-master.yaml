name: pr-to-master

on:
  release:
    types:
      - published

jobs:
  sync:
    uses: circlefin/github-shared-pipelines/.github/workflows/repo-file-sync.yaml@v1
    with:
      repo-ref: test
      config-path: .github/pr.yml
